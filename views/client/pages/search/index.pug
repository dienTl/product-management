extends ../../layout/default.pug 
include ../../mixins/box-head.pug 
include ../../mixins/product-layout.pug
block main 
  .container.my-3
    .row
      .col-12 
        +box-head("kết quả tìm kiếm")

    if(products.length>0)
      +product-grid(products)
    else 
      h2(class="text-center") Không có sản phẩm tìm kiếm